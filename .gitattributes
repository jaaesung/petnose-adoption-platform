# 기본: 텍스트 파일은 LF로 정규화
* text=auto eol=lf

# shell script는 반드시 LF (CRLF면 실행 불가)
*.sh text eol=lf
*.ps1 text eol=crlf

# YAML/Dockerfile
*.yaml text eol=lf
*.yml  text eol=lf
Dockerfile text eol=lf

# 바이너리
*.jar binary
*.png binary
*.jpg binary
*.jpeg binary
